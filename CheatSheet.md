A couple of things that SQL can do, compiled in one file.

1. How to rank things
Part of analysing a data, there are always questions like, Who are the top users? Which platform performs best? Where are the most visited country?
Stuff like that. In SQL you can just use ``` sql ORDER BY```
